<Window x:Class="TinyUrl.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TinyUrl.WPF.ViewModels"
        Title="🔗 Tiny URL"
        Height="650" Width="900"
        Background="#F0F2F5"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#667EEA"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button"
               BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#FFF5F5"/>
            <Setter Property="Foreground" Value="#E53E3E"/>
        </Style>

        <Style x:Key="CopyButton" TargetType="Button"
               BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EDF2FF"/>
            <Setter Property="Foreground" Value="#667EEA"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="🔗 Tiny URL"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#1A1A2E"
                   Margin="0,0,0,20"/>

        <!-- Generate Card -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                Padding="24"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#000000"
                                  Opacity="0.08"
                                  BlurRadius="12"
                                  ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <!-- Input Row -->
                <StackPanel Orientation="Horizontal"
            Margin="0,0,0,12">
                    <StackPanel Width="500">
                        <TextBox Height="40"
                 Padding="12,8"
                 FontSize="13"
                 BorderThickness="1"
                 VerticalContentAlignment="Center"
                 Text="{Binding LongUrl, 
                     UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="BorderBrush" Value="#DDD"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasUrlError}" 
                                     Value="True">
                                            <Setter Property="BorderBrush" 
                                    Value="#E53E3E"/>
                                            <Setter Property="Background" 
                                    Value="#FFF5F5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Validation Error -->
                        <TextBlock Text="{Binding UrlError}"
                   Foreground="#E53E3E"
                   FontSize="11"
                   Margin="0,4,0,0"
                   Visibility="{Binding HasUrlError,
                       Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>

                    <CheckBox Content="Private"
              IsChecked="{Binding IsPrivate}"
              VerticalAlignment="Top"
              Margin="16,10,16,0"
              FontSize="13"/>

                    <Button Content="Generate"
            Style="{StaticResource PrimaryButton}"
            Command="{Binding GenerateCommand}"
            Height="40"
            VerticalAlignment="Top"/>
                </StackPanel>

                <!-- Generated URL Result -->
                <StackPanel Orientation="Horizontal"
                            Visibility="{Binding HasGeneratedUrl, 
                                Converter={StaticResource BoolToVis}}">
                    <Border Background="#F0F7FF"
                            CornerRadius="8"
                            Padding="12,8"
                            Margin="0,0,12,0">
                        <TextBlock Text="{Binding GeneratedUrl}"
                                   FontSize="13"
                                   Foreground="#667EEA"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <Button Content="📋 Copy"
                            Style="{StaticResource CopyButton}"
                            Command="{Binding CopyCommand}"
                            CommandParameter="{Binding GeneratedUrl}"
                            Height="36"/>
                </StackPanel>

                <!-- Error Message -->
                <TextBlock Text="{Binding Error}"
                           Foreground="#E53E3E"
                           FontSize="12"
                           Margin="0,8,0,0"
                           Visibility="{Binding HasError,
                               Converter={StaticResource BoolToVis}}"/>

                <!-- Success Message -->
                <TextBlock Text="{Binding SuccessMessage}"
                           Foreground="Green"
                           FontSize="12"
                           Margin="0,8,0,0"
                           Visibility="{Binding HasSuccess,
                               Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Border>

        <!-- Search Box -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="16"
                Margin="0,0,0,12">
            <Border.Effect>
                <DropShadowEffect Color="#000000"
                                  Opacity="0.05"
                                  BlurRadius="8"
                                  ShadowDepth="1"/>
            </Border.Effect>
            <TextBox Height="36"
                     Padding="12,8"
                     FontSize="13"
                     BorderBrush="#DDD"
                     BorderThickness="1"
                     VerticalContentAlignment="Center"
                     Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>

        <!-- URLs List -->
        <Border Grid.Row="3"
                Background="White"
                CornerRadius="12"
                Padding="16">
            <Border.Effect>
                <DropShadowEffect Color="#000000"
                                  Opacity="0.08"
                                  BlurRadius="12"
                                  ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Public URLs"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,12"/>

                <!-- Loading -->
                <TextBlock Grid.Row="1"
                           Text="Loading..."
                           Foreground="#888"
                           FontSize="13"
                           Margin="0,0,0,8"
                           Visibility="{Binding IsLoading,
                               Converter={StaticResource BoolToVis}}"/>

                <!-- List -->
                <ListView Grid.Row="2"
                          ItemsSource="{Binding Urls}"
                          BorderThickness="0"
                          Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#EEE"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="14"
                                    Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- URL Info -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ShortUrl}"
                                                   Foreground="#667EEA"
                                                   FontWeight="SemiBold"
                                                   FontSize="13"/>
                                        <TextBlock Text="{Binding OriginalUrl}"
                                                   Foreground="#666"
                                                   FontSize="11"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock
                                            Text="{Binding Clicks,
                                                StringFormat='{}{0} clicks'}"
                                            Foreground="#888"
                                            FontSize="11"
                                            VerticalAlignment="Center"
                                            Margin="0,0,12,0"/>
                                        <Button Content="📋 Copy"
                                                Style="{StaticResource CopyButton}"
                                                Command="{Binding DataContext.CopyCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding ShortUrl}"
                                                Margin="0,0,8,0"
                                                Height="30"/>
                                        <Button Content="🗑 Delete"
                                                Style="{StaticResource DangerButton}"
                                                Command="{Binding DataContext.DeleteCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding ShortCode}"
                                                Height="30"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Window>